// возвращает массив после добавления элемента в его конец (push)
function myArr_Push(arr, val) {
    arr.push(val);
    return arr;
}

// возвращает массив после удаления последнего элемента (pop)
function myArr_Pop(arr) {
    arr.pop();
    return arr;
}

/////////////////////////////////////////////////////////////////